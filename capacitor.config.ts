import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.topstudio.skewermaster",
  appName: "skewermaster",
  webDir: "dist/web",
};

export default config;
